{
  "name": "async-validator-next-docs",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vitepress dev --host",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "wrangler pages deploy .vitepress/dist --project-name async-validator-next"
  },
  "dependencies": {
    "async-validator-next": "workspace:*",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.15",
    "wrangler": "^4.54.0"
  },
  "turbo": {
    "pipeline": {
      "build": {
        "outputs": [
          ".vitepress/dist/**"
        ]
      }
    }
  }
}
